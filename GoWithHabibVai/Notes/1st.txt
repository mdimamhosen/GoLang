every program must have a package
every main function package must be main package

import 'fmt'
    fmt => format

func => function
    like func main(){

    }

variable declare in go in this way :
    a := 10
